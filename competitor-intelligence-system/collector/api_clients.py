from __future__ import annotations


def fetch_by_api(platform: str, shop_url: str) -> list[dict]:
    """Placeholder for official platform APIs.

    Return empty list in MVP so the scraper falls back to deterministic mock data.
    """
    return []
